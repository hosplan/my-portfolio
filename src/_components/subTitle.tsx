export default function SubTitle({ subTitle }: { subTitle: string }) {
  return (
    <div className="w-full mt-2 md:mt-5 text-left text-gray-100 font-light md:text-xl">
      {subTitle}
    </div>
  );
}
